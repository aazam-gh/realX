# Backend deployment boundary

This directory contains legacy development copies. Production Cloud Functions
are owned and deployed exclusively from `realX-backend`.

The web Firebase configuration deploys Hosting only. Do not deploy backend code
from this repository.
