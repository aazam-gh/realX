# Backend Ownership

Production Firebase infrastructure is owned by the sibling `realX-backend`
repository. This package is retained for the current `realx-dev` deployment
path only.

Use `firebase.dev.json` for dev. The root `firebase.json` is intentionally
Hosting-only and must remain safe for production use.
